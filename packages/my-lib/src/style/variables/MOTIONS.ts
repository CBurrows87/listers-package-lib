import { motion } from '../constants';

export const MOTION = {
    '--motion-transition-duration': motion.duration,
    '--motion-transition-timing-function': motion.timing,
    '--motion-transition': motion.transition,
};
